import ModelFlow from "@/components/model/ModelFlow";

export const metadata = { title: "구인 공고 — MODELNOW" };

/** 디자이너가 올린 구인 공고 → 모집 상세 → 지원서 */
export default function RecruitPage() {
  return <ModelFlow />;
}
